# Installation

Keras was installed leveraging our pre-existing Conda installation.

```{r k-installation,eval=FALSE}

install.packages("keras")
library(keras)

# No GPU available
install_keras(method = "conda", tensorflow = "default")

```

```{r k-load-libraries}

library(tidyverse)
library(keras)

```


$$

$$

